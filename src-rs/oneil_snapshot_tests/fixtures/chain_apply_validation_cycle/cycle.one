# Override `x` so it now reads `y`. Combined with `y = 2 * x` on the
# leaf this produces a cross-instance cycle, surfaced as a
# `ParameterCycle` validation diagnostic by the post-build SCC pass.

design leaf

x = y
